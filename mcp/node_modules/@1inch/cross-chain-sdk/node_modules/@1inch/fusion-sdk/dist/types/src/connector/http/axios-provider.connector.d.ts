import { HttpProviderConnector } from '@1inch/cross-chain-sdk/node_modules/@1inch/fusion-sdk/dist/types/src/connector/http/http-provider.connector';
export declare class AxiosProviderConnector implements HttpProviderConnector {
    private readonly authKey?;
    constructor(authKey?: string | undefined);
    get<T>(url: string): Promise<T>;
    post<T>(url: string, data: unknown): Promise<T>;
}
